## Database Setup

1. Create a database:
   CREATE DATABASE car_rental;

2. Select the database:
   USE car_rental;

3. Import the SQL file:
   SOURCE database/schema.sql;

4. Make sure your database credentials in the project match:

   * username: root
   * password: your_password
